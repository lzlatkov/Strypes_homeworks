
def fibonacci_sequence(n, memo=None):
    if memo is None:
        memo = {0: 0, 1: 1}

    def fib(k):
        if k in memo:
            return memo[k]
        memo[k] = fib(k - 1) + fib(k - 2)
        return memo[k]

    return [fib(i) for i in range(n)]


print(fibonacci_sequence(10))
