
my_list = input().split()

is_sorted = True

for i in range(len(my_list) - 1):
    if my_list[i] > my_list[i + 1]:
        is_sorted = False
        break

if is_sorted:
    print("sorted")
else:
    print("unsorted")
